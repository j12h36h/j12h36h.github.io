# LCS v0.7.6.1 — Desktop Account Button Restore

- Restores the persistent **Account** button in the regular LCS top-right header.
- The button opens the existing Account view whether signed in or signed out.
- No authentication, Firestore, live-feed, popup, tagging, moderation, avatar, or desktop layout logic was otherwise changed.
- This package targets `/logicalcommunicationservice/`; the separate `/lcs-mobile/` build is untouched.
